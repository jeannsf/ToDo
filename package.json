{
  "name": "mosaicq-to-do-app",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "ts-node ./server/server.ts",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4",
    "react-icons": "^5.4.0"
  }
}
